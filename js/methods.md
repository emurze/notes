# Methods

```
[ARRAY]

Array.from(args)

.unshift() - add elem to start
.shift() - remove elem from start

.push() - add elem to end
.pop() - remove elem from end

removed_elems = arr.splice(0, 2, "an apple")

.concat(arr2)
.slice(1, 2)
.forEach((elem, index, ...args) => {}, arg1, arg2, arg3)

.indexOf(item, position)
.lastindexOf(item, poition)

.includes()                                   - COOL

.find()

.findIndex()
.findLastIndex()

.filter()
.map()
.sort()

.reverse()
.split()
.join()


[STRING]

.at(-2)
.toUpperCase()
.toLowerCase()

.includes()                                   - COOL

.startswith()
.endswith()
.slice()

.trim() – removes (“trims”) spaces from the beginning and end of the string.
.repeat(n)
```
